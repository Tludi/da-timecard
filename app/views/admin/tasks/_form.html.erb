<%= form_for([:admin,@task]) do |f| %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_area :description %>
  </div>

  <hr>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
